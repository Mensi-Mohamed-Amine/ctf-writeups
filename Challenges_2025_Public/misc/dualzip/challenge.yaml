version: "0.1"
id: dualzip
name: dualzip
category: misc
description: |
  zip it!

  Author: ssparrow

hosting:
  type: http
  containerPort: 8000
  readOnlyRootFilesystem: false

connection_info: ${url}

tags:
  - hard

files:
  - ./publish/src.zip

flags:
  - DUCTF{what_the_z1p_just_happened_38442190}
